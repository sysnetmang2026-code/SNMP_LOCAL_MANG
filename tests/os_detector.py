import os
import platform

""" nombre = os.name #retorna nt, aun no entiendo el significado

system = platform.system() #nombre del sistema, ej Windows
release = platform.release() #version, ej 11

#tratar de obtener la version de un remoto

print(nombre)

print(system)
print(release) """

